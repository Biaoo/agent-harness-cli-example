[project]
name = "agent-harness-cli-example"
version = "0.1.1"
description = "Example workspace that uses the agent-harness CLI workflow controller for an AI-IE research task."
requires-python = ">=3.11"
dependencies = []
